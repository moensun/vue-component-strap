var expose = require('../../..');


describe('expose() under node_modules', function() {

    it('should load lib dir', function() {
        var imports = expose();
        imports.should.have.property('a');
        imports.should.have.property('b');
        Object.keys(imports).length.should.equal(2);
    });
});